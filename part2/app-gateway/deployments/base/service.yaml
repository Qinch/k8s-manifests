apiVersion: v1
kind: Service
metadata:
  name: app-gateway
spec:
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30123
  selector:
    app: app-gateway
