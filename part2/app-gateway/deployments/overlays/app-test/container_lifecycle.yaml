apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-gateway
spec:
  template:
    spec:
      terminationGracePeriodSeconds: 60
      containers:
        - name: app-gateway
          lifecycle:
            preStop:
              exec:
                command:
                  - sh
                  - -c
                  - "sleep 30" 
